[project]
name = "pixel-hash"
version = "0.1.0"
description = "A metadata-insensitive SHA-256 content hash for JPEG files"
readme = "README.md"
authors = [
    { name = "Ben Evans", email = "ben@bluechimp.io" }
]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = ["pytest>=8.3.5"]
